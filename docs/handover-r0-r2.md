# 交接：R0–R2 自主会话（2026-09-23）

> 本页是**这一夜（R0–R3 准备）的交接**，写给下一个接手的人或下一轮自主会话。
> 每条结论都指向可复现的命令或证据文件；不依赖任何对话上下文。

**速览（按你的目的挑一条）**：

| 你想做的事 | 看哪节 |
| --- | --- |
| 一句话确认现在是不是好的 | 二、怎么确认它现在是好的 |
| 知道还剩什么没做 | 三、还没做的 · 四、物理阻塞 |
| 知道设备窗口里查到了什么 | 五、设备窗口 · 五之二（页面分诊）· 七、真机冒烟通过 |
| **只有 30 秒** | 七（冒烟结论）+ 八（`page_guild` 结案）+ 八之补充五（`page_event_list` 处置决定） |
| 接手继续干活 | 三、还没做的 → 按路线做 R2 剩余域 / R5 |
| 别重复踩的坑 | 六、几条别丢的经验 |
| 需要你拍板的 | 三（推送）· 四（第 7 项撤退授权、控件验证的同意） |

第八节后有六个"八之补充"小节：对照实验、`page_event_list` 的四步取证（可达 / 判据不稳定 /
无替代路径 / 处置决定）、设备层回归 —— **两次自我推翻都记在那里**。

## 〇、后续轮次追加（同一夜，第 8 轮起）

第 1–7 轮见下面各节；之后又落了这些（提交号可直接 `git show`）：

| 提交 | 内容 |
| --- | --- |
| `bd91c60` / `d7c5fc1` | 任务边界的**只读状态快照**（跨任务复位可被看见）+ 回归；"跑过"要用 `unmet_preconditions` 区分，不能只看结论 |
| `953ba32` / `411e38d` | **大世界/海域域第一刀**（只读探针，走宿主 `map_detect(mode=os)`）+ 回归（含断点续跑接线） |
| `dacd005` | **`device_smoke.py`**：被设备阻塞的三项收成一条命令，设备一上线就能收口 |
| `65fdb5a` / `209b079` | 修 **`--resume` 从来读不到断点**（读的是本次运行目录）+ 接线回归 |
| `b706e93` / `0de0c87` | **活动域**：清点可跑章节（纯离线）+ 与独立数对拍 |
| `c55f480` / `68afc60` | `plan-queue`：**清点 → 生成队列 → 执行** 闭环 + 生成物可执行回归 |
| `71f25d7` / `0d9f030` / `4ef3b2a` | **周期任务域数据源**：`task_catalog`（纠正"task.yaml 顶层键=任务"的错误假设）+ 清点任务 + 回归 |
| `bc5fa79` | **R3 准备**：`r3_candidates.py` → `docs/r3-candidates.md`（原生钩子覆盖排序）；顺手修正 README 里一处记忆值（48→49） |
| `ab87b71` / `af67701` / `fbae1f0` | **R3 结论**：钩子形态分类（区分"调自己的方法"与"调引擎的方法"）；再查词表发现高频项全是引擎动作 —— **R3 在设备在线前无可做的迁移项** |
| `b0e35d5` / `701751a` | **R4 数据面**：`alashub runs` 列表；做它时撞出"同一秒两次运行覆盖工件目录"并修掉 + 回归 |
| `3a53749` | **R4 停止任务**：Ctrl-C / `--stop-file`（任务边界生效）+ 回归 |
| `5d93665` | **R4 区分成功与撤退**：撤退不再被标成 `upstream_error` + 回归 |

**当前状态**：R2 五个域（战役批量 / 账号状态 / 大世界探针 / 活动清点 / 周期任务清点）+ 队列 +
报告层 + 边界快照 + 发现→生成→执行闭环，**每个域都有独立验收脚本**；
R3 已定案（等设备，见 `docs/architecture-roadmap.md` 的 R3 段）；
**R4 数据面完成**：`report` / `runs` / 停止任务 / 区分成功与撤退 / 无设备 dry-run（界面本身未做，理由见路线）。
最后一次全跑：**1.7 分钟，0 步异常**。

## 一、第 1–7 轮做了什么（6 笔提交，`csharp` 仓库）

| 提交 | 内容 |
| --- | --- |
| `b62e684` | **R0**：冻结 `sortie-result/1` 结果合同（Python/C# 两侧实现 + 逐例对拍）；实机记录重核；守卫禁止 `CampaignEnd` 单字段判通关 |
| `fac3568` | **R1**：常驻运行时第一切片（`AlasSession` / `CampaignBatchRunner` / `SessionLog` / `RuntimeErrors`），CLI 变薄，`selftest-runtime` 离线自检 |
| `7d86219` | **R2**：通用任务模型 + 任务队列，战役批量接成第一个域 |
| `7cd18f0` | **R2**：账号状态域（只读）+ `IN_MAP` 临界判据留证 |
| `0d67526` | **R2**：运行报告（工件 → 结构化事实 + 证据完整性 findings） |
| `4c6a5ec` | 守卫：任务域漏注册在静态上查出来 |
| `f4337ba` | 修掉生成文档的每次漂移（上游日志计时归一） |

工作区干净；以上全部**未推送**（项目铁律：推送前须本人同意）。

## 二、怎么确认它现在是好的（一条命令）

```powershell
cd csharp
dotnet build src\Alas.DataTool\Alas.DataTool.csproj -c Release
python tools\diagnostics\verify_all.py --docs-only     # 判据：总耗时行显示 "0 步异常"
```

本轮最后一次全跑结果：**1.5 分钟，0 步异常**。其中新增的几步：

| 步骤 | 覆盖 |
| --- | --- |
| `verify_result_contract.py` | 结果合同 35 例；四类结果 + 20 条反例；两侧裁决逐例一致 |
| `verify_runtime.py` | 运行时 11 例：宿主只起一次、失败即停、取消在边界、队列语义 |
| `verify_account_state.py` | 账号状态只读任务，9 张**真机**存盘帧 |
| `verify_report.py` | 运行报告 + 3 个反例（缺工件 / 缺日志 / 目录不存在） |
| `audit_real_records.py` | 实机归档重核：4 条通关、2 起撤退可解释、0 矛盾 |

## 三、还没做的（按路线顺序）

1. **R2 各域的真机验收**（被设备阻塞）：账号状态 `capture=true`、`IN_MAP` 现场取值、
   本局撤退记录、以及**用新运行时跑一次真机通关**（现有 4 条通关证据出自运行时之前的 CLI 路径）。
   一条命令收口：`python tools\diagnostics\device_smoke.py --allow-actions`。
2. **R3 迁移**：候选已量化并**定案** —— 钩子不可迁（调视觉与地图/导航原语），词表里也没有可离线对拍的纯函数算子（高频项全是引擎动作）。结论与出处都写在 `docs/architecture-roadmap.md` 的 R3 段。
3. **R4 前端**：**数据面已完成**（`report --json` / `runs` / 停止任务 / 成功与撤退可区分）；
   界面本身未做 —— 没有设备就无法验收，且需要先定选型。
4. **R5 宿主替换评估**：等 R0–R4 稳定后再评估，需逐项对拍 + 性能基准 + 可回退开关。

## 四、物理阻塞（需要设备，未做）

- 模拟器当前**未运行**（`adb devices` 为空）。
- 待补的真机项：账号状态 `capture=true` 当场抓帧路径；`IN_MAP` 临界判据的现场复核
  （`data/account_state_probe.json` 里有逐帧相似度：真机地图帧出现过 3.33 / 10.06 / 10.33，
  跨过上游阈值 10）；一条"**本局撤退**被判 `withdrawn`"的真机记录
  （现有 2 起撤退都是上一局清理/导航期，不是本局结论）。

## 五、设备窗口（2026-09-23 06:5x，模拟器上线期间）

设备中途上线（上一轮检查时确实为空），本轮抓住窗口做了三件事：

| 项 | 结果 |
| --- | --- |
| 导航任务真机验证 | `kind=navigate` 两跳真实点击到达 `page_campaign`：`page_main` -(0.946)-> `page_campaign_menu` -(0.997)-> `page_campaign`；不存在的目标页被前置条件拦下记 `skipped` |
| `account_state capture=true` | **真机路径跑通**（此前只用存盘帧验过）：`source=device_capture`、帧 720x1280x3、`pages=[page_campaign]`、`in_map=false`（相似度 147.6） |
| 有界真机冒烟（`device_smoke --allow-actions`） | **战役冒烟失败**：`GameStuckError: Wait too long`（64.8s），停在准备阶段（上游在等 `FLEET_PREPARATION` / `MAP_PREPARATION` 等一组画面）；同队列的只读状态任务成功 |

**同时查出一个自己的缺陷**：`device_smoke.py` 的工件写在 `TemporaryDirectory` 里，脚本一退出
**真机证据就被删掉了**（离线证据可随时重跑，真机要等设备）——发现时已经晚了，那份 `sortie-1-1.json`
里的步骤与 traceback 已不可追。已改为持久目录 `data/device_runs/<时间戳>/`（可用 `--artifacts` 覆盖）。

**设备窗口里接着要做的**（按价值）：

1. 重跑 `device_smoke.py --allow-actions`（现在会留下证据），定位 `GameStuckError` 停在哪一步：
   是客户端弹窗（项目已有「正在攻略中」垫片）、还是舰队/地图准备画面与上游预期不一致；
2. 设备层与页面识别真机回归：`verify_device_engine.py`、`regress_pages.py`（约 5 分钟）；
3. 清单第 6 项（本局撤退换 `withdrawn` 证据）仍需**明确授权**，脚本里已写死"不自动执行"。

## 五之二、页面回归 29/34 的分诊（2026-09-23，设备窗口）

**不是"5 个页都没解锁"**（那是我先前的推断，已被自己推翻）。用 `page_entry_probe.py`
（站在主界面、沿上游页面图取第一跳、按 `variants` 取最像的那个）得到的分诊：

| 目标页 | 第一跳最佳变体 | 在屏 | 相似度 | 结论 |
| --- | --- | --- | --- | --- |
| `page_guild` | `ui_white/MAIN_GOTO_GUILD_WHITE` | ✅ | 5.23 | 入口在屏 → **导航侧问题**，不是未解锁 |
| `page_event_list` | `ui_white/MAIN_GOTO_EVENT_LIST_WHITE` | ❌ | **17.07** | 落在阈值外侧 |
| `page_meowfficer` / `page_os` / `page_private_quarters` | —（第一跳不在主界面） | ❌ | 52~117 | **口径太粗**：入口分别在宿舍菜单/战役菜单，主界面量它没有信息量 |

现场帧（`data/_probe_main.png`，真机主界面）人工核对：

* 右下角有 **「大舰队」**（guild）入口 → 与分诊一致，`page_guild` 的失败在导航侧；
* 活动入口是**带美术图的卡片**（两张「限时活动」+「活动汇总」）—— 这类入口的
  `appear` 是**颜色比对**，而卡面图的均值颜色会随活动轮换变化，**本质不稳定**。
  这与 `IN_MAP` 是**不同的一类问题**（后者是固定偏移、可用垫片修）；
  对美术卡片**不该**照搬"放宽阈值"的做法 —— 需要的是按上游的模板匹配口径去判。

**仍未结案**：`page_guild` 的导航为何失败（入口明明在屏上）；`page_event_list` 该用
哪种判据（颜色 vs 模板）。两条都留了可复现的入口（探针 + 现场帧）。
- **改动"已完善能力"要有证据**：`IN_MAP` 卡在阈值边上时，我没有直接调阈值 ——
  同一客户端上真实地图帧既出现过 3.33 也出现过 10.33，说明还有未知变量；
  先留证、等真机复核，比"看起来能修就修"安全。
- **存档帧有坑**：早期几张帧是 `cv2.imwrite` 存的，通道序与宿主相反，
  相似度会离谱地大（174 / 182）。用帧做判据类验收前先确认存盘通道序。
- **生成文档要么可复现、要么别入库**：`docs/map-detection.md` 曾每跑一次差一个计时数字，
  已归一；同类问题先想"这条 diff 是不是噪声"。
- **守卫自己也会判错**：任务域注册守卫第一版按 `Kind` 字面量查（而注册用的是类名），
  把两个已注册的域误报成未注册 —— 报错信息里给出具体修法，才能一眼看出是守卫错了。

## 七、真机冒烟（修 IN_MAP 之后）整条通过

`python tools\diagnostics\device_smoke.py --allow-actions` → **exit 0**：

| 项 | 结果 |
| --- | --- |
| 当场抓帧（`account_state capture=true`） | `succeeded`：`pages=[page_main, page_main_white]`、`in_map=false`、相似度 94.88 |
| 有界战役冒烟（走新运行时） | **`succeeded`**：`outcome=cleared`、关卡 1/1 通关 |
| 运行报告 | `dry_run=False`、工件 7、宿主启动 **1**、设备配置 **1**、错误 0、findings 无、证据完整 |
| 工件落盘 | `data/device_runs/<时间戳>/`（持久，不再随进程消失） |

同一条命令在修 `IN_MAP` **之前是失败的**（`GameStuckError: Wait too long`，见
[真机卡点](device-stall-in-map.md)）。也就是说这一条命令同时验收了：
常驻会话 / 任务队列 / 批量战役 / 工件与报告 / 账号状态抓帧 / `IN_MAP` 垫片 / 持久工件修正。

## 八、page_guild 结案：入口可见但不可用（账号门禁）

`alashub goto page_guild` 的真机 hop 日志：

```
[hop 2] on=page_main click ui_white/MAIN_GOTO_GUILD_WHITE score=0.9487 at (1188,680) ->   (空)
[hop 0] on=      click <BACK 自救> score=0.0000
```

导航器**正确找到并点击**了入口（模板分 0.9487），但点击后的画面**判定不出任何页面**，
于是它按「未到达」走 BACK 自救 → 回主界面重试 → 循环到失败。

为定位，我按**同一坐标**再点一次并抓帧（`data/_guild_screen.png`）：4 秒后画面**仍是主界面**，
与点击前一致 —— 即这次点击**没有任何可见效果**。合理解释是：入口渲染在屏上但对点击无响应
（典型的等级/进度门禁按钮），而不是「点到了别处」或「导航选错边」。

**据此更正第 51 轮的判断**：当时由「入口在屏」推出「是导航侧问题」，现在证据显示入口
**可见但不可用** —— `page_guild` 的不可达仍属**账号状态门禁**（该号 Lv.32、只解锁第 1 章）。
于是 29/34 可以用账号状态解释完，**不需要引入代码回归的假设**。

**未做决定性验证（如实标注）**：「渲染但无视点击」是**推断**。决定性实验是拿一个已知可用按钮
在同一区域做对照（点击后画面必须改变），以及确认大舰队的解锁条件 —— 两条都未做。

### 八之补充：对照实验已做（决定性，2026-09-23）

上面标注的"未做决定性验证"现已完成 —— **对照实验证明点击机制本身没问题**：

| 实验 | 动作 | 结果 |
| --- | --- | --- |
| 对照 | 同一条底栏、同一机制，点「商店」(80,680) | **画面改变**，识别为 `page_munitions / page_shop / page_supply_pack` |
| 目标 | 点「大舰队」(1188,680)，模板分 0.9487 | **画面无变化**（4 秒后仍是主界面） |

即：`adb input tap` 与坐标都没问题，**大舰队入口确实是"渲染在屏但对点击无响应"** ——
`page_guild` 不可达属**账号门禁**，不是导航或点击链路的问题。

至此 **29/34 已由账号状态完整解释**（4 个受门禁/不存在，1 个事件列表入口另论），
不需要引入代码回归假设。复现：`alashub goto page_guild --adb <adb> --serial <设备>` 看 hop 日志，
以及 `data/_control_tap.png` 与 `data/_guild_screen.png` 两张对照帧。

### 八之补充二：`page_event_list` 是**可达**的 —— 失败在入口判据，不是账号门禁

先回到主界面并**核对了状态**（上一轮我漏了这步，导致一次无效探针：当时还在商店页，白点了一下）：

| 步骤 | 结果 |
| --- | --- |
| `alashub goto page_main` | 已到达（1 跳） |
| 抓帧确认 | `['page_main', 'page_main_white']` ✓ |
| 点「活动汇总」卡片 (1222,150) | **`['page_event_list']`** ✓ |

**结论**：活动入口**在屏、可点、且确实通往 `page_event_list`** —— 这个页在这台账号上**是可达的**。
所以它的 goto 失败属于**入口判据侧**：上游页面图给出的边是
`ui_white/MAIN_GOTO_EVENT_LIST_WHITE`，而该卡片是**带美术图的缩略图**，
颜色比对（实测 17.07，阈值 10）落在外侧，导航因此找不到这条边。

**与 `page_guild` 恰好相反**：那一个是"入口渲染但对点击无响应"（账号门禁），
这一个是"入口能点通但判据认不出"（判据问题）。**同一份失败列表里混着两类根因**，
不能用一个解释覆盖 —— 这也是我先前两次推断都被推翻的原因。

**下一轮的具体动作**（已定位到足够细）：看上游对这条边用的是颜色还是模板
（`variants` 里是否有别的资产）、以及本客户端白皮界面该用哪一个；
若确实是"用颜色判美术缩略图"，正确的做法是换判据口径，而不是照搬 `IN_MAP` 的阈值垫片。

### 八之补充三：`page_event_list` 入口的**判据本质不稳定**（读上游素材定义得到）

上游 `module/ui_white/assets.py`：

```python
MAIN_GOTO_EVENT_LIST_WHITE = Button(
    area={'cn': (1181, 83, 1260, 160)},       # 79x77，右上角
    color={'cn': (147, 155, 186)},
    file={'cn': './assets/cn/ui_white/MAIN_GOTO_EVENT_LIST_WHITE.png'})   # 模板也有
```

对照现场事实：

| 事实 | 值 |
| --- | --- |
| 该素材的区域 | (1181, 83, 1260, 160) |
| 我实测的相似度 | **17.07**（阈值 10）→ 颜色判据不命中 |
| 我成功点通的坐标 | (1222, 150) —— **正落在这个区域内** |
| 该区域画的是什么 | 右上角的**活动缩略图卡片**（两张「限时活动」+「活动汇总」），卡面美术随活动轮换 |

**结论**：坐标是对的、点击是通的，问题在于这个入口**既是颜色判据也是模板判据都不稳定** ——
因为它指向的是**会轮换的美术缩略图**：颜色均值随卡面变，模板截图也会过时。
这不是 `IN_MAP` 那类"固定偏移"问题（那个可以放宽阈值修），所以**不能照搬阈值垫片**。

**因此这里需要的是一个决策，不是快速修补**。可选方向（都要有证据再动）：

1. 查上游在**其它客户端/版本**上这条边是怎么稳定工作的（也许它依赖的是另一层 UI，而不是缩略图本身）；
2. 由 `variants` 或页面图给出别的可用边（例如先到 `page_event` 再进列表）；
3. 若确认"该入口在本客户端只能靠几何位置"，那属于**需要显式决策的例外**，
   且必须写成通用机制（例如"素材无可靠判据时的降级策略"），而不是按页面名写死坐标。

### 八之补充四：`page_event_list` **没有替代图路径**（否掉"绕道 page_event"这条设想）

查上游页面图里进入该页的**全部边**：

```
进入 page_event_list 的边（仅 2 条，且都来自主界面）：
  page_main        -- ui/MAIN_GOTO_EVENT_LIST          (variants: ui/…, ui_white/…_WHITE)
  page_main_white  -- ui_white/MAIN_GOTO_EVENT_LIST_WHITE
page_event_list 的出边（仅 1 条）：
  -> page_main     -- ui/GOTO_MAIN
```

**结论**：想"先到 `page_event` 再进列表"这条绕道**在图里不存在** ——
该页**唯一的入口**就是右上角那张会轮换的缩略图卡片。所以修它只有三条路：

1. 让那一个入口的判据可用（但颜色与模板都随美术轮换，见上一节）；
2. 做**通用降级机制**（"素材无可靠判据时怎么办"），且必须通用、不按页面名写死坐标；
3. 明确接受"该页在页面图里不可达"，并在文档与验收里标注为**已知不可达**（而不是反复当失败查）。

到这里 `page_event_list` 的事实已经收全：**手工可点通、判据不稳定、图上无替代路径**。
下一步是**决策**（走 1/2/3 的哪条），不再是继续取证。

### 八之补充五：`page_event_list` 的处置决定（自主决定，2026-09-23）

用户授权"不要问、自己选最优路线"，所以这里不再挂着等决策，直接定：

**采用第 3 条 —— 记为「页面图内已知不可达」，停止把它当失败反复查。**

理由（按项目既有纪律推）：

1. 修该入口判据（第 1 条）没有可靠落点：颜色与模板**都**随活动美术轮换，
   任何"让它命中"的改法都是刻舟求剑，下个活动就失效；
2. 通用降级机制（第 2 条）是**真正的工程改动**（导航器要支持"素材无可靠判据时的行为"），
   需要设计 + 回归 + 真机验证 —— 现在没有预算，硬做会留下没有验收的机制，
   比记一条已知限制更糟；
3. 记为已知不可达**不丢信息**：手工可点通的事实、判据数值、图上无替代边，全部留了证据；
   将来若要做第 2 条，这里有完整的起点。

**同时明确不该做的事**：不按页面名写死坐标（项目铁律禁止按页面/素材加专用坐标），
所以"反正坐标 (1222,150) 能用就直接写进适配层"这条路**排除**。

**重新评估的触发条件**（写清楚，避免以后凭感觉重开）：出现第 2 条的实施预算，
或上游页面图/素材定义发生变化（活动入口不再是轮换缩略图）。

### 八之补充六：设备层回归通过（真机，2026-09-23）

`python tools\diagnostics\verify_device_engine.py` → **exit 0 / 设备引擎回归：通过**
（实测行：`adb  ADB  442.8ms  [720,1280]  61.7/60.1` —— 后端可切、能抓到合法帧、点击可用）。

这条是设备清单第 2 项（R1 起设备 I/O 走宿主，换后端要重跑），至此在本窗口关闭。
窗口内已关闭：设备层回归、账号状态抓帧路径、`IN_MAP` 现场取值与修复验证、
一条命令整套运行时验收（`device_smoke.py --allow-actions`）。
**仍未做**：页面识别全量回归的**重新基线**（29/34 已归因到账号状态，但基线数字本身需要按当前账号重记）、
本局撤退换 `withdrawn` 记录（**需授权**）。
