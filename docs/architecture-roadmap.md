# 迁移路线与当前状态

最终目标：C# 执行全部非视觉逻辑，Python 仅保留必要的 CV/OCR 桥接。当前仍是混合运行时，重写尚未完成。
R0 证据、R1 运行时、R2 任务模型与 R4 UI 为现有基础；主线转为拆除产品路径的业务 Python 依赖、完成 C# 引擎与任务执行。R3 钩子和 R5 宿主替换是必须交付的实现工作，不再只做可行性评估。具体调用归属和迁移顺序见[引擎重写](upstream-engine-rewrite.md)。

## 阶段门槛

| 阶段 | 当前成果 | 剩余工作与验收门槛 |
| --- | --- | --- |
| R0：真值与证据 | 冻结 `sortie-result/1`，Python/C# 对拍；归档主线、活动成功结算和本局撤退 | 其他结果分支仍需对应真实样本，不能从离线覆盖推断真机全覆盖 |
| R1：常驻运行时 | 单会话、取消、断点、逐任务工件和报告；主战役/大世界地图观测与原生导航有真机样本 | 其他设备后端、地图条件和平台验证 |
| R2：任务域 | 已注册 15 种任务，但多数游戏任务仍转发 Python；通用队列属于 C# | 全部域内状态机与业务行为迁到 C#；逐域验实际效果，注册与分派成功不算完成 |
| R3：钩子迁移 | 旧计划链仍有缺口；独立新工程已直接编译 1-1 至 1-4 的声明和钩子、公共调度及循环 | 完成全部规则、公共行为与继承语义，在新执行图中验证，不接回旧计划链 |
| R4：产品 UI | 六主题、Home、界面设置、任务编辑、统计、配置管理、开发者工具及系统/远程设置已集成；部署设置接 Core，平台部署草稿和资源偏好已接线，更新/登录组件已纳入验收 | 资源拖拽、远程服务、更新/认证与跨平台发布尚未全部接通；真实桌面/浏览器/GPU 性能待验，页面集成不等于业务完成 |
| R5：宿主替换 | Python 宿主仍混合视觉、业务和设备职责，C# 引擎未完成产品接线 | 收窄 Python 为视觉桥；非视觉业务、设备与状态由 C# 执行，禁用业务 Python 后通过真实产品验收及性能对照 |
| R5 进展（C# 引擎侧，仍未满足门槛） | 已集成计划导出、原语、选择、寻路、循环和宿主合同的离线回归；当前严格递归调用审计统计 6,515 个步骤、74 个不完整钩子、7 个缺少 MRO 绑定的 super 编码失败、12 个需原生绑定解析的调用，调用覆盖和注册表覆盖仍失败，不能将其计为全量执行。影子观测已隔离到本次原生运行，但只比较已观测轮次的钩子选择。 | 原语真机动作层、完整调用绑定、性能、真实 C# 成功结算与回退证据仍缺；生产继续走上游 `CampaignRun.load_campaign()` 和原生 `Campaign.run()`。源缺陷、计划拒绝、调用编码失败和设备验证分别记账，不能合并成“0 阻塞” |

R5 舰队属性分派已通过原生 Fleet 属性与双宿主的 592 场景离线对照：同队或未配置二队不切换，已导出的实例覆写优先于同名原语，不完整覆写仍拒绝。未导出继承、原语内部动态覆写和真机行为仍待核对；此专项不抵消表中缺口。

清敌与清路障组合方法已恢复原生返回语义，避免末端返回假时多执行兜底；312 个原生离线对照及渠道空结果拒绝通过。只验证指定状态下的控制流与动作调用，不增加真机通关或宿主替换证据。

神秘格与舰队保护循环已移除录制宿主的一轮截断；15 个原生逐动作状态对照及 3 个预算拒绝通过。神秘格仍有 100 次实验动作预算，超限未结束会明确阻塞。复合扫描已移除自建 Fleet 代理及前缀/顺序/同权目标豁免，改用原生属性、C# 注册原语并比较返回值、完整参数和状态；4,080 例中有 681 例差异或未验证，检查保持失败。这些是待定位的对照缺口，包含替身/集合顺序/原生输入失败，不能全部计作引擎缺陷，也不能豁免为通过。

新架构整体重建，禁止把新模块逐个接回旧结构：独立引擎/编译规则 → C# 设备与权威状态/纯视觉服务 → 完整导航、战役和各任务域 → 桌面与 Server 组合入口整体切换 → 旧流程从发布依赖图移除。静态数据和溯源校验保留。现有架构检查中的永久原生调用要求须随整体切换改为语义与执行归属验收；历史成功证据保留为原生基线，不能改标成 C# 成功。

`Alas.Engine.slnx` 已提供不引用旧 Core 的独立构建：首批四张主线规则、类型化章节覆盖、每局隔离状态、普通/全清/低地图信息三种分派、十次移动重试与二十轮循环均直接执行 C#。原生实际 Campaign 方法对照 18,628 个合成末端动作场景通过；25 项本地/进程检查覆盖依赖隔离、状态隔离、参数、并发流、超时、取消、输出超限、ADB 动作拒绝、前台应用判断与设备方向。进程输出超限的原始错误已保留，不再误报超时。

独立纯视觉服务已实现彩色/亮度/二值模板匹配、GIF 逐帧测量、全图素材裁剪和 RGB 均值。C# 管理常驻进程、帧与请求身份、严格阈值、动画首个命中/末帧失败偏移、超时/取消及错误收尾；颜色判据也在 C#。Python worker 只依赖图像库，不导入上游模块，不解析素材 id，不接设备/页面/任务业务；越界裁剪保留上游补黑语义。21 项合成像素及协议反例通过，包含错帧拒绝、并发请求、超时/取消/释放、根因保留、模板尺寸拒绝与业务调用拒绝。OCR 仍明确未实现，不回退旧宿主；缩放、地图特征和原生完整视觉路径尚未验收。

45 个上游模块的 1,793 个素材及 53 个页面关系已直接迁为可编译 C# 声明，保留四服参数、来源行/源码哈希与图片哈希；运行时不读取导出 JSON。构建期 `tools/migration/compile_static_rules.py --upstream .runtime/engine --check` 检查源码漂移，只支持声明迁移，未知控制流拒绝。四服实际原生对象对照通过 7,172 项素材、212 项页面、10,192 项可达最短路径与 1,040 项页面识别调用轨迹；2,499 项 C# 合成导航通过。原生同长路径用无序集合择路，因此比较可达性、最短长度和声明边，不声称同长路线顺序固定一致。630 项原生/C# 视觉对照覆盖所有页面检查素材的静态/GIF 帧、移位和空白图，核对颜色、匹配结果与点击偏移。

`UiRecovery` 已直接迁移通用页面恢复、弹窗优先级、紧急委托热更新检查、剧情跳过与选项确认、主界面/大世界弹窗、撤退加载等待、休眠页面和点击后间隔重置。四服实际原生方法对照通过 716 个场景、2,008 次状态转换、508 项间隔重置及 32 张剧情选项图像；末端识别/设备为测试替身，比较完整调用顺序、参数、错误和多帧计时。剧情色带检测以参数调用纯 CV，选项几何、索引、确认和动作在 C#；动态选项没有导出步骤或解释器。应用健康检查保持上游前台窗口/活动判断，不把后台进程存在当作游戏正在运行。方向查询保留上游未报告时按正常方向处理，并单独记录是否真实报告。

独立 `Alas.Engine.Cli` 的 `observe` 禁用动作，`navigate` 通过 `Runtime/NavigationRun` 组合 C# 设备、规则、恢复与导航；CLI 只解析参数和输出。每次运行保存图像哈希、导航观察、逐动作尝试/完成状态，失败保存调用栈并登记失败帧；取消与期限到期分别保留。8 项端到端离线重放使用实际 CV/子进程、上游素材截图和合成 ADB，覆盖到达、只读、点击失败、取消、超时及非标准尺寸拒绝。这不是设备验收：本轮 ADB 枚举未发现连接设备，未进行实机截图、点击或出击。发布依赖只含新引擎、CLI 和纯视觉 worker，不包含旧 Core；Python 图像库需由指定环境提供。

这仍不是可用的新产品：`ICampaignOperations` 尚无真实游戏动作实现；地图观测/寻路/交互、其余规则、完整任务队列、配置/统计、结算证据与桌面/Server 切换均未完成。设备缩放/旋转归一化、原生卡死/连续点击检测和其他设备后端尚未迁完，当前 UI 明确拒绝非 1280×720 图像，并在动作前保存失败证据。完整游戏内导航仍待真机效果与性能验收，`navigate` 只是新架构的独立诊断入口。新循环结束值不表示通关，不生成 `cleared`。验证命令如下；本地无上游时可省略参数执行独立检查，原生对照和纯 CV 检查明确记为未跑，不能据此通过完整门槛。

```powershell
dotnet build Alas.Engine.slnx -c Release
dotnet run --project tests/Alas.Engine.Tests -c Release -- --python .runtime/venv314/Scripts/python.exe --upstream .runtime/engine --artifacts .runtime/verification/native-csharp-engine
dotnet run --project tests/Alas.Engine.Tests -c Release -- --vision .runtime/venv314/Scripts/python.exe .runtime/verification/native-csharp-engine
dotnet run --project tests/Alas.Engine.Tests -c Release -- --ui .runtime/venv314/Scripts/python.exe .runtime/engine .runtime/verification/native-ui-engine
dotnet run --project tests/Alas.Engine.Tests -c Release -- --recovery .runtime/venv314/Scripts/python.exe .runtime/engine .runtime/verification/native-ui-recovery
dotnet run --project tests/Alas.Engine.Tests -c Release -- --runtime .runtime/venv314/Scripts/python.exe .runtime/engine .runtime/verification/native-navigation-runtime
```

## 自动化规则覆盖与剩余项

- 全量索引 1,437 个模块：1,367 个可运行原生关卡的 Config 合并、MAP 身份、继承方法与 C# 地图摘要/逐格语义通过；67 个为辅助模块。3 个历史模块因当前上游缺失 `C2`、`D3`、`EVENT_20200312CN_SP3` 素材符号导入失败，严格检查保持失败；导出与漂移一致不能豁免源缺陷，不加逐地图替代规则。
- 1,793 个素材的四服组合共 7,172 项原生加载/字段对照通过；52 个可识别页面四服合成正对照通过，`Page(None)` 明确跳过。原生导航 2,551 个可达图对用合成识别/点击状态通过；真实页面可达性仍受账号、活动、服务器与客户端条件约束。
- 控件扫描包含 31 个模块实例、25 个延迟构造声明和 7 个任务内工厂。四服独立进程共 252 项通过，保留导入时的服务器分支；延迟返回的 Scroll 与容器内 Navbar 均调用原生识别。7 个工厂另有 192 个合成状态场景验证原生切换、附加处理和滚动循环，不算真机命中或业务完成。57 个 Scheduler.Command 绑定及声明依赖通过，11 个仅配置分组另记。
- 导出消费端补齐来源、服务器字段、索引与哈希校验，正常数据及损坏反例共 43 组在 Python/C# 同时验收。完整原生覆盖、地图对照及双端导出完整性检查会因源缺陷退出 1，不能以其他专项通过声称总体通过。
- MAP 导出 2.1.0 补齐符号格子、类引用、复制链与 22 个原生方法声明：1,370 份 MAP、8,322 个赋值字段均可追溯；10 组静态回归和原生导入赋值/调用逐项对拍通过。JSON 始终是离线元数据，未改战役加载/导航/实战语义。
- Campaign 声明导出 2.2.0 补回 18 个被静默丢弃的类/格子引用，保留自身 913 个数据属性、9 个方法别名和来源类型；7 组回归与原生类字典对拍通过。3 个历史入口缺失不再伪装为静态完整，校验明确失败；不把声明摘要当作有效继承状态或运行时数据源。
- 当前导出器 2.11.0 另外保留计划控制流、方法签名、参数类型和来源元数据；严格调用审计以 6,515 个步骤、7 个 super 编码失败复核，仍对 74 个不完整钩子和 12 个待原生绑定调用保持失败。离线执行器与覆盖统计也拒绝把 super 归为同名原语：标准夹具实调 31/34 个原语，3 个未覆盖；状态写入审计另有 39 项待核对，均不能抵消或豁免。
- `8be3c3e` 完整离线验收为 72 项通过、7 项设备检查跳过、2 项失败，耗时约 7.3 分钟；地图对照与全量原生覆盖仍暴露上述 3 个源缺陷。此前 69/7/3 整轮记录原样保留，其中实例绑定夹具问题已修复，本轮 14/14 通过。单独运行 Python/C# 导出完整性也明确拒绝同 3 项，不能把漂移一致等同于源可运行。
- 全部 57 个周期任务与 7 个独立工具经真实原生配置绑定、run() 和入口方法执行，256 个离线分派场景通过，覆盖普通返回、False、TaskEnd 与原生 Restart 重试写入。最终领域类/函数和设备使用替身，未验证领域内部业务，详见[全量分派报告](archive/reports/native-dispatch.md)。
- 工具真实构造器另有 28 个场景，复现并修复内部设备工厂绕过会话、DaemonBase 禁用检测残留两项通用问题；7 个工具的原生任务初始化、会话设备复用、未配置拒绝与正常/异常恢复通过。领域末端 run 仍为替身，不算真机工具效果。
- 设备作用域扩展到周期任务和每次连续调度：16 个嵌套规划/扫描场景由全失败修为全通过，覆盖缓存有无、异常、原有检测方法及两次连续分派；避免内部工厂另建设备或把禁用检测留给后续任务。原生构造器与规划调用保留，末端 I/O 为替身。
- 原生截图 `auto` 曾被永久覆盖项反复重置，首次基准写回后即落到 ADB 默认分支。统一绑定改为保留上游解析值，6 组/18 个实际原生配置与截图分派观测通过。缺省导航/抓帧入口也改用临时 override，避免写回会话选择的设备后端；真机两任务队列复验成功，配置字节不变。修复前的后端写回已比对并还原，原始证据保留；未测真实后端速度。
- 连续调度的类级囤积状态曾从上一轮残留，令下一轮应再等 5 分钟的任务提前执行；现按原生新进程语义在整轮调度初始化并恢复，不改变轮内原生选择。真实 Config/loop 的 5 个离线场景覆盖连续运行、既有 False 和异常恢复，等待仅观测、不实际执行。
- 任务目录补回 `task.yaml` 的 9 组/68 个成员，并与生成目录逐项核对，Core 工件保留完整分组。损坏或缺失来源及集合漂移明确失败；独立工具成功响应增加身份、授权、原生计划/目标与错误一致性校验，47 个 Core 队列场景通过，失败不进入断点完成列表。
- 调度状态原先将字符串 false 当作启用，并把缺 Enable 折算为禁用；现保留存储快照语义，显式值严格要求布尔、缺省标为未知，非法字段在过滤/截断前报错。Core 核对快照来源、字段、完整/截断计数及条目，拒绝矛盾响应并保留原文；20 个宿主输入/结构、25 个 Core 响应/断点场景及配置只读、报告回归通过。未套用原生默认或锁定开关，不代表实际调度计划。
- `config_get` 与 `task_schedule` 现按队列输入的规范实例名读取对应上游原始配置，默认仍为 `alas`；调度快照不再接受任意配置文件路径，调度任务的 Core 拒绝宿主返回的实例身份漂移。双实例合成配置、非法名称、缺失实例及只读字节对拍通过，临时配置已清理；这不改变原生有效调度语义。
- 周期任务一次性覆盖曾因上游校验先看默认类型，拒绝岛屿生产效率合法的浮点选项 `0.04/0.12`；现以精确匹配的 option 类型调用同一原生校验，其余选项、范围、只读和高级模式规则不变。三字段浮点覆盖及原有 6 组覆盖回归通过，未访问设备或写入配置。
- 战役收尾修复结算后异常仍被判成功及历史错误遮蔽新异常的问题；真正逃出原生 run 的异常记为 error，已恢复错误不覆盖后续结算或限额。24 个 S3 离线测试、38 例 Python/C# 合同对拍通过，词表与通关判据不变，不增加真机通关数量。
- S3 曾将已有非默认实例设备与默认 alas 配置混用；地图配置现按会话所属实例重新读取，S3 先核对设备身份再调用原生加载器，避免旧任务临时覆盖泄漏及设备后端写回。四个新增场景先全失败后通过，完整 13 项加载、14 项实例绑定及截图/结果合同回归通过；未改章节合并、MAP 或原生战斗，未新增真机通关结论。
- 活动清点和队列生成按导出 Campaign/MAP 结构排除辅助模块，默认活动候选由 879 项修正为 840 项，保留的 39 个支持模块仍可溯源。全量生成列表、分层、only_complete、空集与生成队列 dry-run 回归通过；候选不表示历史活动当前可用或真机通关。
- 周期任务、连续调度、独立工具补齐全新宿主的原生数值兼容，并隔离上一关显式全清选项；三个独立进程验证原生交点计算、拒绝路径与正常/异常恢复，不依赖先执行地图探针。
- 原生 Main 实测舰队选择重复点击，定位为任务入口漏装已有战役兼容；统一上下文现按原生加载/运行调用准备舰队、相机及战斗兼容，结算颜色作用域限定于战役 run。16 项原生离线检查由 3 项失败修为全通过，配置/MAP/次数/返回语义保持原生，不增加地图或任务特例。
- 周期任务的一次性覆盖补齐原生字段校验与值转换，修复字符串 `"false"` 被领取分支视为真值、日期未转换的问题；无效类型、选项与只读字段在设备获取前拒绝。5 项隔离回归及 7 组已归档覆盖输入通过，不据此推断实际领取效果。
- 独立工具及连续调度补齐失败摘要的调用栈、原生根因和失败帧传递；恢复或收尾异常追加而不覆盖原错误。原生分派、重试和边界取消未变，合成故障回归不算真机业务完成。
- 原生页面导航补齐错误摘要调用栈、SystemExit 隔离和逐段失败帧，保留设备最后缓存帧与来源；落盘失败不覆盖原原因。未改页面图、识别、点击或弹窗流程，合成失败回归不算真实页面操作成功。
- 导航入口曾继承上一任务的检测历史，在第一张截图前误报 GameStuckError；现于任务边界清空 stuck/click 记录。原生 UI/Device/Timer 四场景先复现失败再通过，当前导航的卡死和重复点击保护仍有效；拒绝路径不获取设备或清记录，未新增真机页面成功结论。
- 原生导航连续抓帧曾在页面过渡时误选返回边，并把短暂命中目标的过渡帧报为到达。宿主仅在 `UI.ui_ensure()` 期间等待原生点击动画稳定，返回前用新帧和上游判据复核；完整产品队列真机回归为 30/34，军需和活动列表通过，剩余四页未达保留原始失败，不按页面另建路线。
- 任务边界证据改读已有原生设备缓存，避免记录上一页宿主帧；来源明确、无缓存不回退、读取不新增设备动作。6 项宿主隔离检查及 3 种 Core 会话通过；原生识别和通关合同不变。
- 关卡初始化失败不再暴露上一关对象；原生导入、设备、配置、绑定、加载器、首帧及输入失败共 7 个反例先失败后通过。12 项加载回归验证重新初始化恢复，未改原生章节合并、MAP 或调度，未增加真机通关结论。
- 通用 OCR 探针修正 `letter` 的 RGB 类型及缺省值，独立透传 `threshold/alphabet`；7 组原生预处理/四服语言/错误回归与 C# 协议通过。原生任务 OCR 和模型未替换，合成输入不作为真机识别准确率证据。
- 入口复核删除旧固定坐标撤退线程和独立 C# 导航点击算法。原生入口/撤退调用链保持；历史残留出击弹窗在当前原生素材判据中未命中，该状态尚未完成适配，需补上游支持与现场原生流程复验，见[入口证据](archive/reports/native-campaign-entry.md)。
- 本地上游历史确认 3 个历史入口素材在 `8eab19ab049c2ee277b433ecaa54008e533a0ced`（2020-04-09）已删除，旧关卡仍保留导入；不是本次导出遗漏。剩余工作是核对这些已退役依赖的上游支持方式，并按真实可用入口补各业务结果证据；不能拼接不同年代源码后声称当前客户端可用。当前设备已授权并完成 1-4 原生成功结算及返页复验；未覆盖范围仍保留缺口，既有成功结算证据保留。详细范围及内容哈希见[全量规则报告](archive/reports/upstream-coverage.md)。

## R2 验证边界

- 主线统一流程有 1-1、1-4、2-1、2-2 成功结算样本；活动有 A1/A2/A3 样本，未覆盖的章节不据此推断。2-2 新样本保留原生继承 Config、battle_0/battle_3 钩子和战斗调度，约 169 秒取得成功结算后返回章节页，合同无违例、账号配置字节不变；未增加地图特例。
- 1-2 另取得原生 `battle_2`/`clear_boss`、S 级成功结算及 `sortie-result/1` 的 `cleared` 样本；同队列实时抓帧为 `page_campaign`、`in_map=false`，合同 0 违例，账号配置字节哈希不变。入口的退出游戏弹窗先由上游 `handle_popup_cancel()` 关闭；这份样本不证明其他章节可通关。
- 1-3 使用相同通用 `campaign_batch` 入口和上游 `Campaign.run()`，继承 1-1 的章节 Config，原生 `battle_0`/`battle_2` 经三场战斗取得 S 级成功结算；`sortie-result/1` 为 `cleared` 且 0 违例，同队列实时抓帧确认 `page_campaign`、`in_map=false`。1-2、1-3 的原件均保留在本地忽略目录，脱敏工件进入结果审计；两次运行前后账号配置 SHA-256 不变，均不外推到未验章节。
- 2-3 沿同一入口继承 2-1 Config 与章节 CampaignBase，实际执行上游 BOSS 可达性判断及战后相机恢复，四场战斗后取得 S 级成功结算；结果合同 `cleared`、0 违例，后续实时抓帧为 `page_campaign`、`in_map=false`。脱敏结果已归档，配置 SHA-256 前后相同；不外推其他章节。
- 2-4 继续由上游处理道路阻塞、潜在阻塞、BOSS 可达性和战后相机恢复，章节 Config 在 2-1 继承字段上合并自身两项边缘参数；四场战斗后取得 S 级成功结算、合同 `cleared` 且 0 违例，后续实时抓帧返 `page_campaign` 且不在图内。脱敏结果归档通过，账号配置 SHA-256 不变；不能据此证明其他关卡或自动周回。
- 3-1 继续走同一生产入口，原生章节 Config 八项及 `battle_0`/`battle_3` 钩子生效；舰队前推、救援、谜题清理与 BOSS 调度后，四场战斗取得 S 级成功结算、合同 `cleared` 且 0 违例，后续实时抓帧返 `page_campaign` 且不在图内。脱敏结果归档通过，账号配置 SHA-256 不变；未验关卡仍保留缺口。
- 3-2 继承 3-1 的章节 Config，原生舰队救援、谜题清理、BOSS 后相机恢复及剧情跳过弹窗均在现场发生；四场战斗后 S 级成功结算、合同 `cleared` 且 0 违例，后续实时抓帧返 `page_campaign` 且不在图内。脱敏归档通过，账号配置 SHA-256 不变；不外推未验关卡。
- 3-3 合并 3-1 与自身的九项有效章节 Config，原生战斗钩子、舰队救援、谜题清理、BOSS 后相机恢复与剧情跳过在现场执行；四场战斗后 S 级成功结算、合同 `cleared` 且 0 违例，后续实时抓帧返 `page_campaign` 且不在图内。脱敏归档通过，账号配置 SHA-256 不变；不外推未验关卡。
- 3-4 合并 3-1 与自身的八项有效章节 Config，保留章节 MAP 与谜题载具覆盖；四场原生战斗及 BOSS 后相机恢复后取得 S 级成功结算、合同 `cleared` 且 0 违例，后续实时抓帧返 `page_campaign` 且不在图内。脱敏归档通过，账号配置 SHA-256 不变；不外推未验关卡。
- 4-1 使用自身六项章节 Config 及 `Campaign` 地图覆盖属性，经原生谜题清理、BOSS 可达性与相机恢复后，四场战斗取得 S 级成功结算、合同 `cleared` 且 0 违例，后续实时抓帧返 `page_campaign` 且不在图内。脱敏归档通过，账号配置 SHA-256 不变；同轮控制台出现一键退役与重入关卡，逐点击轨迹未归档，不能据此独立核算退役结果或推断其他关卡。
- 4-2 继承 4-1 的六项章节 Config，原生谜题清理、BOSS 可达性与相机恢复后四场战斗取得 S 级成功结算、合同 `cleared` 且 0 违例；后续实时抓帧返 `page_campaign` 且不在图内。脱敏归档通过，账号配置 SHA-256 不变；未验章节与周回仍留缺口。
- 4-3 合并 4-1 与自身的八项有效章节 Config，原生战斗钩子、谜题清理、BOSS 可达性与相机恢复后四场战斗取得 S 级成功结算、合同 `cleared` 且 0 违例；后续实时抓帧返 `page_campaign` 且不在图内。脱敏归档通过，账号配置 SHA-256 不变；未验章节仍保留缺口。
- 4-4 合并 4-1 与自身的六项有效章节 Config，原生道路阻塞、潜在阻塞与 BOSS 可达性流程执行后五场战斗取得 S 级成功结算、合同 `cleared` 且 0 违例；后续实时抓帧返 `page_campaign` 且不在图内。脱敏归档通过，账号配置 SHA-256 不变；未验关卡仍保留缺口。
- 5-1 使用自身六项章节 Config，由上游谜题清理、BOSS 可达性与相机恢复流程完成五场战斗并取得 S 级成功结算，合同 `cleared` 且 0 违例；后续实时抓帧返 `page_campaign` 且不在图内。脱敏归档通过，账号配置 SHA-256 不变；其他关卡仍保留缺口。
- 5-2 继承 5-1 的六项章节 Config，原生谜题清理、伏击回避、BOSS 刷新后相机恢复与战斗调度完成五场战斗，取得 S 级成功结算、合同 `cleared` 且 0 违例；后续实时抓帧返 `page_campaign` 且不在图内。脱敏归档通过，账号配置 SHA-256 不变；其他关卡仍保留缺口。
- 5-3 合并 5-1 与自身的七项章节 Config，原生谜题清理、BOSS 刷新后相机恢复与战斗调度完成五场战斗，取得 S 级成功结算、合同 `cleared` 且 0 违例；后续实时抓帧返 `page_campaign` 且不在图内。脱敏归档通过，账号配置 SHA-256 不变；其他关卡仍保留缺口。
- 5-4 合并 5-1 与自身的九项章节 Config，原生道路阻塞、潜在阻塞、BOSS 可达性及相机恢复流程完成五场战斗，取得 S 级成功结算、合同 `cleared` 且 0 违例；后续实时抓帧返 `page_campaign` 且不在图内。脱敏归档通过，账号配置 SHA-256 不变；同轮船坞容量处理只记执行痕迹，不独立断言退役结果。其他关卡仍保留缺口。
- 原生导航有战术学院往返、待机恢复及主页进入章节页样本；2026-09-25 再验 1-4 四场战斗后 S 级成功结算，合同无违例，随后同队列抓帧确认 `page_campaign` 且不在图内。旧 C# 导航器的逐跳记录不证明当前路径。
- 当前账号完成舰队、船坞、委托、演习四页各两轮原生导航，逐页抓帧确认后回到主页；只证明页面识别与往返，不证明编队、退役、委托派遣或演习战斗成功。
- 当前账号又完成商店、建造、邮件、科研四页原生导航、逐页抓帧及主页返页，13 个队列任务通过且用户配置逐字节不变；不证明购买、建造、邮件领取或科研项目执行。
- 修复边界帧来源后另验科研往返的 5 个任务，`device_cached` 边界与随后实时抓帧页面一致，结束在主页且不在图内，账号配置逐字节不变；它只证明此轮导航与帧来源，不代表科研项目执行。
- `observe(map=os)` 有 4/4 抓帧与网格检出的真机正样本；主战役观测已补可选原生章节 Config 透传，1-1 地图中连续 4/4 检出 7 格、无执行错误，随后通过上游导航返回主页。准备仅运行到原生 map_init，未战斗，不计通关；其他地图和通用配置负样本不据此改判。
- `reward` 有领取点击与奖励弹窗证据，但无独立到账数量读数；`dorm`、`freebies`、`tactical`、`meowfficer` 的调度样本不能一概视为领取、购买或补书成功。
- `daily` 已在当前低等级账号验证海域突进：原生战斗出现 S 级结算，再经原生快速挑战与奖励弹窗返回 `page_daily`，本地次数画面从 3/3 到 0/3；未独立核对奖励到账数量。另一护卫任务被上游识别为 inactive，仅证明调度返回，不算任务完成或战斗失败。
- 每日困难任务进入 1-1 后原生地图初始化报 `No vertical line detected`；该功能要求周回，本次无自律选项。补齐主线任务一次性 `mode=normal/hard` 后，1-1 困难开荒通过原生章节配置、相机恢复及两场战斗，取得 S 级成功结算并返页；每日周回及其他困难章节仍待验收。此前失败记录保留，周期任务 SystemExit 丢失根因/失败帧的问题已修复并经真机复核，详见 [S3 说明](s3-upstream-adaptation.md)。
- 已通关账号的大世界入口与调度有样本；要塞长战斗的通用接管修复已有离线回归，自动化成功结算闭环仍待验收。
- 未解锁“自律寻敌”的账号不满足上游大世界动作前提；入口超时不能据此外推为通用界面缺陷。
- 原生连续调度有一次真实边界停止样本：独立临时配置关闭全部 Reward 领取开关，原生导航往返后正常延后任务，再响应停止；队列保留 cancelled、断点 completed 为空。后续抓帧确认主页且不在图，用户配置逐字节不变。它不证明领取或其他调度任务完成，见[调度证据](archive/reports/scheduler-evidence.md)。
- 一次性覆盖真机复验在临时配置中将 5 个 Reward 领取开关由本地 true 覆盖为 false，原生进入 Reward 页后回主页，未进入领取循环；覆盖未持久化，用户配置字节不变，原生 NextRun 更新仍保留。首次夹具先抓帧绑定默认实例，随后切换临时实例被正常拒绝；修正调用顺序后的两任务队列通过。原始失败与成功工件均保留，本项不证明奖励到账。
- 原生 Main 新宿主路径修复后完成四场战斗，S 级结算日志后返回章节页，RunCount 从 1 减到 0 停止；三任务队列通过、用户配置字节不变。约 175.8 秒的 Main 工件和后续实时帧已脱敏归档；该入口尚无逐关结果合同，不能据分派成功增加合同通关数量。修复前舰队选择重复点击的失败证据保留。
- 周期任务与大世界动作的原生 `decision=ran` 现在经过统一响应合同：任务/实例、授权与确认、构造/运行/成功标记及目标模块、类、方法、命令必须完整一致且无错误；缺失或矛盾响应记为 `contract_violation`，保留宿主原始证据，不进入断点 `completed`。52 项 Core 队列回归通过；这仍只证明分派合同，不证明领域业务完成。

真机事实由[结果审计](archive/reports/result-evidence.md)和[队列审计](archive/reports/queue-evidence.md)生成；
识别覆盖见[识别总账](archive/reports/status.md)。历史轮次和性能调查见[归档](archive/README.md)。

## 下一步

已复现的导航任务记录、S3 实例混用和调度快照类型问题已修复。继续处理上述上游源缺陷、
残留出击弹窗和未完成的真实业务验证；不把离线规则覆盖或原生分派返回当作全量游戏验收。

UI 性能测试已增加显式隔离模式：桌面 `--ui-only`、网页 `?ui-only=1` 在组合入口选择内存数据，
不创建真实后端或控制 API 客户端，关闭后台轮询，保留共享视图交互和手动日志负载。配置、启停、主题与资源偏好仅在当前内存生效；
Headless 覆盖隔离工厂、全部模拟能力和输入导航，真实窗口/浏览器性能未验证。此功能不替代 R2 自动化业务验收。

任务配置页卡顿定位为内置 Noto CJK 首次布局约 6.35 秒，以及每次切换整页重建。系统中文字体优先、内置字体兜底并缓存最近三个完整任务页后，
真实上游 Main/Event 各 54 字段、1757/1743 个控件仍默认构建；Release Headless 首次完整打开为 423/388 ms，稳态切换中位 8.4/8.1 ms、约 22 KB/次。
字段输入还曾在每个字符后扫描全部字段行并执行所有字段刷新委托；现已分离字段局部通知、操作区聚合状态和搜索可见性刷新。
Release Headless 真实上游 Main 连续输入 20 次中位 0.107 ms、P95 0.889 ms，且 54 个字段控件仍完整构建。
离屏回归覆盖第四页淘汰、事件退订、草稿和筛选恢复，不能把这些数字外推到真实桌面窗口或浏览器。

游戏自动化优先按上述源缺陷与真实业务证据缺口推进；以下 UI 待办由并行任务负责。UI 性能验收包含批量日志与稳态追加、跟随/暂停、离树重挂接和订阅回收；批量改善伴随稳态显著退步时不集成。

1. 已验收的 UI 切片、颜色控件及日志性能修复已集成，保留主线 Core 接线、六主题布局与模态修复。日志按视口复用控件，补齐订阅生命周期、窄屏变高日志跟随与暂停竞态；资源展示值未变化时复用集合与卡片，避免每份日志快照重建资源控件。相同隔离入口前后分配下降 55.0%–76.7%，主线重跑每次约 0.31–0.83 MB。暂停阅读再按行身份与像素位置锚定，倒序专项最大漂移 360/560 px 修为 0，分配下降约 71.4%/79.1%；主线独立复验桌面/网页构建、完整 Headless（新增 54 项锚点检查）、23 场景/71 断言、4 个隔离场景和 4 个暂停相位通过，真实窗口/浏览器性能仍待验证。系统/远程设置共用部署分组和草稿队列，更新/登录页及资源选择组件已进入主线；异步旧响应、连续输入失焦、窄屏越界已补回归。资源偏好已通过桌面文件与浏览器本地存储按实例接线，概览设置弹层直接编辑同一份选择，增删和键盘排序即时生效；拖拽手势与现场行为仍待验收。开发者工具的材质、模拟和退出模式仍待接通。详见[统一 UI](r4-ui-architecture.md)。
2. 共享 UI 已接任务配置、周期/独立工具提交、原生连续调度启停、五类统计报告、指挥喵报告和策略校验；桌面直接调用 `Alas.Core`，浏览器通过网络适配器。运行中的原生日志/任务列表/资源记录，以及首次运行前的实例配置总览已接离线验证链；资源卡选择与平台持久化已接通，完整采集和第三方 NTP 校时仍待接通。工具/调度尚未验证真机效果，第三方新增工具仍待完整迁入。配置 JSON 与统计 CSV/PNG 已接平台文件能力，选择取消或读取失败不写入文件；真实浏览器/系统文件选择器仍待现场验证。继续补设置、更新和认证；掉落统计依赖尚未迁移，报告能读不等于游戏采集流程完成。
3. 部署设置的 8 个上游分组、密码只写不读、独立启动运行列表已接 Core 文件事务及桌面/网页能力接口；310 例上游对拍、并发保存、Windows 文件占用与真实 HTTP 客户端通过。宿主保留新增字段、过期冲突与 Python/Core 共用锁，两个上游原始类回归通过。系统/远程页面已接 Core 保存与同会话草稿；桌面进程内存与浏览器 `sessionStorage` 的平台草稿存储已注入，桌面重启不恢复草稿，浏览器现场仍待验。部署器、启动列表执行、远程服务、认证、更新/重启仍待接通，保存设置不会启动这些服务。随后分别验证桌面、服务器、Python 宿主和设备依赖的跨平台发布。
4. 按可用账号和设备窗口补 R2 真实业务证据；设备由主 agent 串行使用，失败先定位上游调用链。

## 后端调用边界

`Alas.Core` 就是业务后端，承载 ALAS 迁移逻辑、任务、调度、状态、控制流程及诊断能力。产品可执行入口只有 `Alas.UI.Desktop` 和 `Alas.Server`：桌面 UI 在同一进程内通过能力接口调用 Core；Server 在其进程内直接调用 Core，并提供 API 与共享 Web UI 的静态托管，服务进程不加载桌面 UI。网页是交给浏览器执行的静态资源，Headless 只用于验收。共享 UI 不复制任务状态机，也不把 HTTP 写死进页面。

## 长期不能变动的规则

- 战役必须使用 `CampaignRun.load_campaign()`、章节 `Config` 合并和原生 `Campaign.run()`。
- 页面、按钮、弹窗、地图和视觉对象来自上游；禁止逐地图、逐界面独立适配。
- 导出 JSON 仅作离线展示、溯源和漂移校验，不替代上游运行时规则。
- 新域先实现 `ITaskRunner`，由 `TaskQueue` 调度；CLI 与前端不复制业务状态机。
- 通关只认结果合同；`CampaignEnd`、撤退、超时、单帧和威胁百分比都不能单独证明通关。
- C# 图像实现只用于参考和对拍；未满足 R5 门槛不替换上游宿主。
- 完整约束与变更验证要求见 [AGENTS.md](../AGENTS.md)。
