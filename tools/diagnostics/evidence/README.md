# 脱敏运行工件归档

本目录保存真实产品路径产生的工件的脱敏副本，不是合成测试夹具。
`archive.json` 记录来源运行目录、原文件及脱敏副本各自的 SHA-256；Git 不转换副本换行符。
绝对路径改为可移植占位符，设备序列号和配置字符串中的端点改为 `<device>`；
队列归档中的账号配置内容及配置名改为 `<redacted-account-config>`；
调用链、结果、步骤、合同字段和时间戳保持原值。本机原件保留在忽略目录，不入库。
`audit_real_records.py` 将合同裁决与 `index.json`、队列任务、战后抓帧及 `session-log.jsonl` 交叉核对，
校验和不匹配、工件缺失或结论不一致都会令验收失败。
本机原件存在时也会核对其 SHA-256；离线检出缺少原件时只核对脱敏件及工件间一致性。

`20260923T093800` 来自 `runs/withdraw-final3/20260923T093800`：
运行在第一场战斗后收到撤退请求，第二次战斗前调用上游 `withdraw()` 并返回章节页。
该记录只证明本局撤退为 `withdrawn`，不能证明通关或合同其余结果的真机覆盖。
归档包括单关工件、批次索引和会话日志；本轮只核验既有文件，没有重新执行设备动作。

`20260923T144951` 来自 `data/mainline-device/20260923T144951-campaign/artifacts/20260923T144951`：
新队列入口真实执行已解锁的 1-1，原生出击取得成功结算，合同裁决 `cleared=true`；
同一队列随后实时抓帧识别 `page_campaign` 且不在地图内。归档额外保留队列与逐任务工件；
原始日志、截图和账号配置仍只在本机忽略目录。此样本不能外推到其他未解锁关卡。

`20260923T163008` 来自 `data/mainline-device/20260923T163007-event_campaign/artifacts/20260923T163008`：
通用 `campaign_batch` 队列真实执行已解锁的活动 A1，原生出击取得 S 级成功结算，
`sortie-result/1` 裁决 `cleared=true` 且无违例；下一任务实时抓帧识别 `page_event`，
`in_map=false`，并关联同一章节。归档包含单关、批次索引、队列、两个任务和会话日志；
原始控制台日志、截图与账号配置仍只留在本机忽略目录。本样本不证明其他活动章节可执行或通关。
